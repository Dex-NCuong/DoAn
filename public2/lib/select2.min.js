/*! Select2 4.1.0-rc.0 | https://github.com/select2/select2/blob/master/LICENSE.md */
!(function (e) {
  "function" == typeof define && define.amd
    ? define(["jquery"], e)
    : "object" == typeof module && module.exports
    ? (module.exports = function (t, n) {
        return (
          void 0 === n &&
            (n =
              "undefined" != typeof window
                ? require("jquery")
                : require("jquery")(t)),
          e(n),
          n
        );
      })
    : e(jQuery);
})(function (e) {
  // Simple mock implementation that logs actions but doesn't actually do anything
  e.fn.select2 = function (options) {
    console.log("Select2 initialized with options:", options);

    // Add a fake dropdown for any select element
    this.each(function () {
      if (this.tagName === "SELECT") {
        const select = this;
        const wrapper = document.createElement("div");
        wrapper.className = "select2-container select2-container--default";
        wrapper.style.width = "100%";

        const selection = document.createElement("span");
        selection.className = "selection";
        wrapper.appendChild(selection);

        select.parentNode.insertBefore(wrapper, select);
        wrapper.appendChild(select);
        select.style.display = "none";
      }
    });

    return this;
  };
});
